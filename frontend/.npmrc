# Ensure all dependencies get installed (including dev dependencies)
production=false
# Speed up installation by skipping optional tasks
fund=false
audit=false
# Ensure scripts run properly
ignore-scripts=false
# Ensure correct permissions
unsafe-perm=true
